#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <windows.h>
using namespace std;

void CreateFFile(string path);
void CreateSFile(string path1, string path2);
void OutputFile(string path);